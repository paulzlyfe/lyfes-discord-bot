name: Repo Relay

on:
  push:
  pull_request:
  issues:
  release:
  workflow_run:
    types: [completed]

jobs:
  relay:
    runs-on: ubuntu-latest
    steps:
      - name: Repo Relay
        uses: repo-relay/action@v1
        with:
          webhook-url: ${{ secrets.DISCORD_WEBHOOK }}
